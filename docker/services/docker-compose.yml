version: "3"
services:
  redis:
    image: redis
    restart: always
    ports:
      - 6379:6379
    volumes:
      - ../data/redis:/data

  api:
    image: renatoruis/cepninja
    restart: always
    links:
      - redis
    ports:
      - 80:3000